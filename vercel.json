{
  "version": 2,
  "builds": [
    {
      "src": "api/parse_list.py",
      "use": "@vercel/python",
      "config": {
        "maxDuration": 10,
        "includeFiles": "services/**"
      }
    }
  ],
  "routes": [
    { "src": "/api/parse-list", "dest": "/api/parse_list" }
  ]
}
